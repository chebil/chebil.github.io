# Python dependencies for RUNNING the notebooks locally.
#
# These are NOT needed to build the book. The site is built with MyST MD,
# which is a Node tool installed separately:
#
#     npm install -g mystmd
#     myst start          # live preview
#     myst build --html   # what .github/workflows/deploy.yml runs
#
# CI does not execute notebooks — it renders their stored outputs — so this
# file is only for working on the notebooks yourself.
#
#     pip install -r requirements.txt

# Scientific stack
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.5.0
seaborn>=0.11.0

# Machine learning (Part II — chapters 6-10)
scikit-learn>=1.0.0
torch>=2.0.0
torchvision>=0.15.0

# Search, CSP and knowledge representation (Parts I and III)
networkx>=2.6.0          # graph algorithms, chapters 2-3
python-constraint>=1.4.0 # CSP solver, chapter 1
rdflib>=6.0.0            # knowledge graphs, chapter 12
gym>=0.26.0              # reinforcement learning lab, chapter 10

# Notebook environment
jupyterlab>=3.0.0
ipykernel>=6.0.0
ipywidgets>=8.0.0
