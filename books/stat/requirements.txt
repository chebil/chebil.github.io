# Python dependencies for RUNNING the notebooks locally.
#
# These are NOT needed to build the book. The site is built with MyST MD,
# a Node tool installed separately:
#
#     npm install -g mystmd
#     myst start          # live preview
#     myst build --html   # what CI publishes
#
# CI does not execute notebooks -- it renders their stored outputs.
#
#     pip install -r requirements.txt

# Scientific stack
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
matplotlib>=3.5.0
seaborn>=0.11.0
statsmodels>=0.13.0

# Used in single sections
matplotlib-venn>=0.11.0   # set diagrams, chapter 3
scikit-learn>=1.0.0       # chapter 9 applications

# Required by scripts/make_redirects.py
pyyaml>=6.0

# Bayesian chapters reference these but do not depend on them for the
# published outputs. Install only if you intend to re-run those cells.
# pymc>=5.0.0
# arviz>=0.15.0
